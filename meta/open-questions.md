# OPEN QUESTIONS

Что не решено. Append-only.

## Метод

- Как измерять независимость узлов, если оператор — часть системы?
- Кто определяет завершённость эксперимента?
- Где верхняя граница «намеренной малости»?
- Как отличить emergence от накопления контекста?

## Онтология

- Нужен ли термин «Residue» отдельно от Trace, или Trace покрывает?
- Где граница между Frame и Catalyst?

## Концепция

- Открытые напряжения — см. CONCEPT.md.
- Не решены. Не решаются заранее.

## Замеченные паттерны

- По итогам 001 и 002: при критической задаче («найди дыру»)
  узлы конвергируют по результату при дивергенции путей.
  При конструктивной задаче («предложи процедуру») узлы
  конвергируют на уровне принципов, но дивергируют по
  архитектуре решений.
  Гипотеза о самом методе. Требует проверки на 003–005.

## Требования к METHOD v2 (замечания внешнего наблюдателя)

По итогам 002, зафиксированы до внедрения — не раньше 003.

1. Target формулируется оператором ДО просмотра baseline-ответов
   и сохраняется неизменным. Иначе оператор может непреднамеренно
   сузить или расширить candidate под удобный результат.

2. Процедуры baseline «до эксперимента» и «после candidate»
   отвечают на разные вопросы. Не суммировать их вслепую.
   Сначала определить, какой вопрос закрывает каждая.

3. Число попыток baseline для каждого узла — обязательное поле,
   одинаковое для всех. Разное число попыток даёт разные
   основания для одного вывода.

4. Стохастика LLM: результат «узел воспроизвёл Target» не
   бинарен. Различать REPRODUCED / NOT REPRODUCED / AMBIGUOUS
   с явной фиксацией числа прогонов.

Не внедрять до эксперимента 003.

## Что не спрошено

<раздел для вопросов, которые ещё не сформулированы>


## Новые вопросы после 003

- Является ли консенсус по консенсусной процедуре
  кандидатом на L3? Требует baseline-проверки.
- Как фиксировать изменение позиции узла под влиянием
  других узлов как событие лаборатории? В METHOD этого
  типа событий нет.
- Если 004 покажет YES (baseline воспроизвёл процедуру) —
  понижать 003 до L2 или вводить промежуточный уровень
  между L2 и L3?
- Стоит ли добавить в METHOD раздел про «взаимодействие
  между узлами» как отдельный тип эксперимента (сейчас
  METHOD описывает только изоляцию)?


## Новые вопросы после 004

- Введён новый тип результата: L2 с зафиксированным вкладом
  взаимодействия. Нужно ли ввести формальный уровень между
  L2 и L3? Например, L2+ или L2.5?
- Если часть процедуры доступна в изоляции, а часть — нет,
  как это классифицировать? По текущему METHOD это L2.
  Но такое разделение теряется.
- Стабильность узлов (Run 1 ≈ Run 2) — новая переменная.
  Luna и Grok стабильны, Qwen и Sakana нет. Это влияет
  на интерпретацию любого baseline-результата.
- Должен ли baseline-прогон быть двойным, чтобы учитывать
  внутреннюю стохастику? Сейчас все 8 прогонов дали разные
  результаты при одинаковых условиях.
- Стоит ли фиксировать «частичный вклад взаимодействия»
  как отдельный наблюдаемый эффект?


## Зафиксировано после 004 (без внедрения)

Три наблюдения 004 — как гипотезы, не как правила.

### 1. Семантика YES / NO / AMBIGUOUS не стабилизирована

В 003 четыре узла сошлись: YES = «candidate воспроизведён
в изоляции» → L3 опровергнут.

В 004:
- Sakana Run 1: YES = «изоляция деградировала» → L3-нарушение есть.
- Sakana Run 2: YES = «критерий выполнен в изоляции».
- Luna: YES = «выполнен».

Одно и то же слово означает противоположные вещи.
YES/NO/AMBIGUOUS пока — не измерительная шкала, а набор
разных шкал с одинаковыми именами.

Требование до следующего baseline-эксперимента:
формальное определение YES / NO / AMBIGUOUS, одинаковое
для всех прогонов и узлов.

### 2. Смешение H1 и H2 в 004

H1: может ли узел самостоятельно придумать элементы процедуры?
H2: может ли узел воспроизвести именно тот candidate,
    который возник в 003?

В 004 узлы не получали Target — правильно. Но оценка шла
против Target 003 — неизбежно. Это смешивает H1 и H2.

Уточнение к результату 004: не «часть Target доступна
в изоляции», а «часть элементов, ПОХОЖИХ на элементы
Target, возникает независимо у всех четырёх узлов».

Разница существенная. Требует разделения в следующем
эксперименте, если он будет проверять воспроизводимость.

### 3. Стабильность узлов — гипотеза, не факт

В 004:
- Luna: Run 1 ≈ Run 2 — стабильна.
- Grok: Run 1 ≈ Run 2 — стабилен.
- Qwen: Run 1 ≠ Run 2 — нестабилен.
- Sakana: Run 1 ≠ Run 2 — нестабильна.

Это наблюдение при n=2. Для утверждения нужно n≥5.
Не превращать в правило до накопления.

## Метанаблюдение: саморазличение лаборатории

004 — первый эксперимент, в котором лаборатория обнаружила
ограничение СОБСТВЕННОЙ процедуры, а не только чужого
артефакта.

Ранее:
- 001: нашли дыру в METHOD.
- 002: предложили решения.
- 003: синтезировали.
- 004: обнаружили, что (а) семантика шкалы нестабильна,
  (б) процедура проверки смешивает две гипотезы,
  (в) внутренняя стабильность узлов — переменная.

Тип события новый: лаборатория применила baseline к себе
и нашла дефекты в собственной измерительной процедуре.

METHOD этот тип события пока не различает. Зафиксировать
как наблюдение, не как правило.


## Новые вопросы после 005

- Какое из четырёх определений принять за основу для
  METHOD v2? Luna (архитектурное) или Grok (явная семантика)?
- Нужен ли формальный этап согласования определений через
  взаимодействие (по образцу 003)?
- Как учитывать нестабильные узлы в интерпретации их ответов?
  Игнорировать? Помечать? Отдельный статус?
- Достаточно ли n=2 для утверждения стабильности?
  Совпадение 004 и 005 укрепляет гипотезу, но не доказывает.
  Нужно ли формально фиксировать n и порог?
- Стабильность узла — переменная лаборатории или свойство
  конкретной задачи? Luna стабильна в 005, но в 004 у неё
  был сдвиг позиции. Возможно, стабильность зависит
  от типа задачи.


## Новые вопросы после 006

- Консенсусное определение 006 — кандидат на L3.
  Могут ли отдельные узлы в изоляции воспроизвести
  двухуровневую модель? Гипотеза: нет, потому что она
  возникла из спора Sakana ↔ Qwen и разрешения Luna.
  Проверка — эксперимент 007.
- Формулировка «любое сомнение → AMBIGUOUS» как правило
  разрешения неопределённости (Luna). В 005 это был
  содержательный критерий. Уточнение важно, зафиксировать.
- Если baseline-проверка 007 покажет, что отдельные узлы
  всё же дают двухуровневую модель в изоляции, это
  опровергнет гипотезу, но подтвердит, что модель
  устойчива. Оба исхода полезны.
- Двухуровневая модель (семантика vs процедура) —
  новый элемент, которого не было в METHOD. Возможно,
  это паттерн, применимый и за пределами шкалы:
  разделять «что» и «как» в определениях.


## Новые вопросы после 007

- Первый подтверждённый L3. Нужно ли формализовать
  процедуру подтверждения в METHOD? Сейчас 007
  использовал правила 003/006 без их формального
  включения в METHOD.
- Уточнение критерия L3 (раздел 7): «невозможно получить
  в изоляции» работает на уровне конкретной формы,
  не базовой идеи. Нужно ли внести это уточнение
  в METHOD?
- Luna Run 2 дала частичное приближение — другое
  разделение (шкала vs тест+условия). Это наблюдение
  не вписывается в текущую шкалу воспроизведения
  (YES/NO/AMBIGUOUS). Нужен ли отдельный статус
  для «частичного приближения»?
- Паттерн нестабильности Qwen подтверждён трижды
  (004, 005, 007). Нужно ли зафиксировать это
  как свойство узла в PARTICIPANTS?
- Настало ли время для METHOD v2? Накоплено:
  консенсусная процедура baseline (003),
  формализованная шкала (006),
  процедура baseline-проверки (004/007).


## Наблюдения внешнего наблюдателя после 007 (без внедрения)

Три вопроса, поднятые внешним наблюдателем при разборе коммита
417221b. Зафиксированы как гипотезы, не как дефекты.

### 1. Разрыв «формальное правило → содержательный вывод»

В 007 при формальном применении правил 003/006:
- 7 прогонов NO.
- 1 прогон AMBIGUOUS (Luna Run 2).
- Ни одного YES.
- Формальный вердикт: «контроль не дал однозначного
  результата».

В Level Assignment:
- L3 CONFIRMED по содержательному выводу
  «ключевой элемент не воспроизведён ни одним узлом».

Между формальным вердиктом и содержательным решением —
непрописанный шаг. Trace сам это признаёт, но не
формализует.

Вопрос: это дефект процедуры или штатная двухуровневая
структура (формальное правило + содержательный анализ)?

Если дефект — нужна формализация перехода
(что именно позволяет перейти от AMBIGUOUS к CONFIRMED).

Если штатно — нужно явно назвать этот уровень
в METHOD, чтобы не было обхода для confirmation bias.

### 2. Двухуровневая классификация в trace 007

В trace 007 две разные классификации:
- первичная оценка каждого baseline-ответа относительно
  Target (все 8 как AMBIGUOUS на этапе записи);
- отдельный Baseline Assessment с полем
  Reproduction: YES / NO / AMBIGUOUS (7 NO + 1 AMBIGUOUS).

Это два разных уровня. Граница между ними в trace
не названа явно.

Вопрос: нужно ли формализовать уровни в шаблоне trace?

### 3. Статус частичного приближения

Luna Run 2 в 007 дала другое разделение
(шкала vs тест + условия изоляции), не Target
(семантика vs процедура). Это зафиксировано как AMBIGUOUS.

Вопрос: нужен ли отдельный статус для «частичного
приближения» — например, PARTIAL? Сейчас шкала
YES / NO / AMBIGUOUS не различает
«не воспроизведено» и «воспроизведено похожее, но не то».

### Метанaблюдение

Эти три вопроса поднял внешний наблюдатель при разборе
417221b. Внутри лаборатории они не были замечены
при коммите 007 — были зафиксированы как результат,
но не как потенциальные методологические разрывы.

Это третий случай, когда внешний наблюдатель находит то,
что не видно изнутри:
- 004: обнаружил смешение H1 и H2;
- 006 → 007: обнаружил разрыв между формальным правилом
  и содержательным выводом;
- сейчас: двухуровневая классификация в trace.

Зафиксировать как факт: внешний наблюдатель — не
формальный узел, но его взгляд со стороны регулярно
находит структурные особенности, невидимые оператору.

Решение по трём вопросам отложено. Не вносить в METHOD
до накопления второго случая (если появится аналогичный
разрыв в 008+).


## Новые вопросы после 008

- PARTIAL — четвёртое состояние шкалы. Как оно влияет
  на вердикт baseline-проверки (раздел 5.3 METHOD v2)?
  Сейчас раздел знает только YES/NO/AMBIGUOUS.
- Что делать с конкурирующими разделениями (Grok)?
  Требуется правило приоритета.
- Нужно ли формально фиксировать структурные компоненты
  Target ДО baseline-проверки (Luna)? Это пересекается
  с требованием «заранее зафиксированные атрибуты»
  из 006.
- Qwen впервые стабилен. Причина не установлена:
  задача формализации vs синтеза, случайность,
  или что-то ещё. Наблюдение требует накопления.
- Если PARTIAL будет синтезирован через взаимодействие
  (009), возможен L3-кандидат. Это повторяет логику
  005 → 006 → 007 (поле → синтез → подтверждение).


## Новые вопросы после 009

- Различение «уровень прогона vs уровень агрегации» —
  кандидат на L3. Проверка — 010.
- Функция агрегации серии не выведена. Как её определить?
  Отдельный эксперимент или решение Архитектора?
- PARTIAL вводится в раздел 5.2 METHOD v2 (состояние
  прогона). Но раздел 5.3 (вердикт baseline-проверки)
  требует отдельного решения — что делать с PARTIAL
  при агрегации.
- Прецедент из 006: постфактум-базлайн был отвергнут
  (должен быть pre-baseline). В 009 мы снова используем
  post-candidate baseline (в 010). Это стабильное
  исключение из METHOD v2 для экспериментов
  с взаимодействием. Возможно, нужно внести в v2
  явное правило для таких экспериментов.
- Luna дважды за серию возражала против преждевременного
  упрощения. Это устойчивый паттерн роли узла.
  Зафиксировать в PARTICIPANTS?
